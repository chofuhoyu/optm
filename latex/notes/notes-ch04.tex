% ============================================================
%  第4章：对偶单纯形法与灵敏度分析
% ============================================================
\section{第4章 \quad 对偶单纯形法与灵敏度分析}

\subsection{4.1 为什么需要对偶单纯形法}

原始单纯形法走的是"原始可行 $\to$ 对偶可行"的路线：始终保持 $b \ge 0$（原始可行），通过迭代逐步让检验数全部 $\le 0$（达到对偶可行，即最优）。

但在实际中经常遇到\emph{反过来}的情况：已经有一张检验数全部 $\le 0$ 的表（对偶可行），但某些 $b_i < 0$（原始不可行）。此时重头用大M法太浪费——对偶单纯形法正是为这种场景设计的。

对偶单纯形法的核心思想：保持\emph{对偶可行性}（检验数 $\le 0$），逐步消除 $b$ 列的负值，最终达到原始可行——此时既是原始可行又是对偶可行，即为最优解。

典型适用场景：
\begin{itemize}
  \item 灵敏度分析中 $b$ 改变导致基变量取值变负
  \item 新增一个约束后原最优解不再满足
  \item 初始表中检验数已 $\le 0$，但某些 $b_i < 0$（例如 $\ge$ 约束标准化时乘以 $-1$）
\end{itemize}

（课件 p6.19）与原始单纯形法"保原始可行、追对偶可行"对称，对偶单纯形法"保对偶可行、追原始可行"，两者构成完美的镜像。

\subsection{4.2 对偶单纯形法操作步骤}

对偶单纯形法一次迭代：

前提：表中所有检验数 $\le 0$（对偶可行），但存在 $\bar{b}_r < 0$（原始不可行）。

\begin{enumerate}
  \item \textbf{离基选择}：选择 $b$ 列中\emph{最负}值所在行的基变量离基，记为第 $r$ 行。
  \item \textbf{进基选择（对偶最小比值）}：在第 $r$ 行中，对所有\emph{负系数} $\bar{a}_{rj} < 0$，计算
        $\displaystyle \theta_j = \frac{|\sigma_j|}{|\bar{a}_{rj}|}$。
        取 $\theta_j$ \emph{最小}的变量 $x_k$ 进基。\\
        若该行全无负系数，则原问题\emph{无可行解}。
  \item \textbf{转轴}：以 $\bar{a}_{rk}$ 为主元进行行变换，使 $x_k$ 列成为单位向量，同时更新检验数行。
\end{enumerate}

（课件 p6.20）

\medskip
\textbf{原始单纯形 vs 对偶单纯形对比}：
\medskip
{\small
\setlength{\tabcolsep}{2pt}
\begin{tabular}{@{}lcc@{}}
\toprule
& \textbf{原始单纯形} & \textbf{对偶单纯形} \\
\midrule
先选什么 & 进基（最大正检验数） & 离基（最负 $b$ 值） \\
后选什么 & 离基（$\min b_i/a_{ik}$） & 进基（$\min |\sigma_j|/|a_{rj}|$） \\
比值在哪儿算 & 进基列（纵向） & 离基行（横向） \\
保持什么 & 原始可行（$b\ge 0$） & 对偶可行（$\sigma \le 0$） \\
推动什么 & 检验数全部 $\le 0$ & $b$ 列全部 $\ge 0$ \\
\bottomrule
\end{tabular}
}

\subsection{4.3 为什么是这个顺序——因果解释}

\textbf{为什么先选离基（最负 $b$）？}

对偶单纯形法的目标是消除 $b$ 列的负值。最负的 $b_r$ 意味着第 $r$ 个约束被违反得最严重——这个基变量取值为负，离可行域最远。优先修正它能最快地向可行域靠近。这和对偶问题中目标函数是 $\max\ b^T w$ 有关：$b_r < 0$ 时让 $x_{B_r}$ 离基，等价于对偶问题中让目标函数值增加——这正是对偶问题要做的（最大化）。

\textbf{为什么在负行系数上取最小比值？}

假设第 $r$ 行上 $\bar{a}_{rj} < 0$。转轴后 $b_r$ 变为 $\bar{b}_r / \bar{a}_{rj} > 0$（因 $\bar{b}_r < 0$ 且 $\bar{a}_{rj} < 0$）。但其他行 $i \neq r$ 的 $b_i$ 也会受影响：
\[
\bar{b}_i' = \bar{b}_i - \frac{\bar{a}_{ij}}{\bar{a}_{rj}}\bar{b}_r
\]
取 $\theta_j = |\sigma_j|/|\bar{a}_{rj}|$ 最小，相当于在对偶问题中做"最保守的步子"——保证所有检验数都保持在 $\le 0$，不破坏对偶可行性。这与原始单纯形法中"取最小 $b_i/a_{ik}$ 以保证不跑出可行域"的几何直觉完全对称。

\textbf{两者的对称性总结}：原始单纯形在对偶不可行的方向上试探，用最小比值保护原始可行性；对偶单纯形在原始不可行的方向上试探，用最小比值保护对偶可行性。

\subsection{4.4 灵敏度分析总框架}

灵敏度分析回答一个实际问题：当 LP 的参数（价格 $c$、资源量 $b$、工艺系数 $A$）发生变化时，最优解会怎么变？我们从\emph{已求出的最优表}出发分析，而非重头求解。

\textbf{灵敏度分析总决策表}：
\medskip
{\small
\setlength{\tabcolsep}{1pt}
\begin{tabular}{@{}p{36mm}p{20mm}p{42mm}@{}}
\toprule
原问题状态 & 对偶问题状态 & 应对措施 \\
\midrule
可行 + 对偶可行 & 对偶可行 & 最优解/最优基不变 \\
可行 + 对偶不可行 & 对偶不可行 & 原始单纯形继续迭代 \\
不可行 + 对偶可行 & 对偶可行 & 对偶单纯形继续迭代 \\
不可行 + 对偶不可行 & 对偶不可行 & 加人工变量从头求解 \\
\bottomrule
\end{tabular}
}

\subsection{4.5 目标系数 $c$ 的变化}

（课件 p6.46、p6.47）

\subsubsection{情况一：非基变量 $c_j$ 变化}

非基变量 $x_j$ 的 $c_j$ 变为 $c_j'$，只有 $x_j$ 自己的检验数受影响：
\[
\sigma_j' = z_j - c_j' = \sigma_j - (c_j' - c_j)
\]
若 $\sigma_j' \le 0$（$\min$ 问题），最优解不变；若 $\sigma_j' > 0$，以当前表为起点，$x_j$ 进基继续单纯形迭代。

\textbf{为什么只影响一个检验数？} 检验数的计算公式是 $\sigma_j = c_B^T B^{-1} a_j - c_j$。非基变量的 $c_j$ 只出现在公式的第 2 项。$c_B$（基变量价格向量）不变，$B^{-1}a_j$ 不变，所以 $z_j = c_B^T B^{-1}a_j$ 不变——唯一变化的是减去新 $c_j'$。而基变量 $c$ 变化时 $c_B$ 改变，所有 $z_j$ 都要重新算。

\subsubsection{情况二：基变量 $c_k$ 变化}

基变量 $x_k$ 的 $c_k$ 变为 $c_k'$。$c_B$ 变了，\emph{所有}非基变量的检验数须全部重新计算：
\[
\sigma_j' = (c_B')^T B^{-1} a_j - c_j, \quad \forall j \in \text{非基}
\]
还须更新目标值：
\[
z_0' = (c_B')^T B^{-1} b
\]
若所有 $\sigma_j' \le 0$，仍最优；否则继续迭代。

\subsection{4.6 右端项 $b$ 的变化}

$b$ 变为 $b'$ 后，新右端：
\[
\bar{b}' = B^{-1} b'
\]
$b$ 变化不影响检验数 $\sigma$（$\sigma$ 与 $b$ 无关）。

\begin{itemize}
  \item 若 $\bar{b}' \ge 0$：最优基不变，只是基变量取值变了。新目标值 $z' = c_B^T \bar{b}'$。
  \item 若出现 $\bar{b}_i' < 0$：检验数仍 $\le 0$，但对偶可行而原始不可行——用对偶单纯形法恢复可行性。
\end{itemize}

\subsection{4.7 新增约束}

（课件 p6.57）

步骤：
\begin{enumerate}
  \item 将原最优解代入新约束。若满足 $\to$ 最优解不变，结束。
  \item 若不满足：引入松弛/剩余变量，将新约束作为新行添入最优表。
  \item 在新行中消去基变量（用现有的约束行消去基变量在新行中的系数），使新基变量列成为单位向量。
  \item 此时检验数仍 $\le 0$（新松弛变量检验数为 0），但新行 $b$ 可能为负 $\to$ 用对偶单纯形法。
\end{enumerate}

对偶单纯形法的操作要点：保持 $\sigma \le 0$，消除 $b < 0$——先选最负 $b$ 离基，在负系数行上按 $\min |\sigma|/|a|$ 选进基。

\subsection{4.8 新增决策变量}

新变量 $x_{n+1}$ 的 $c_{n+1}$ 和系数列 $a_{n+1}$ 已知：

\begin{enumerate}
  \item 计算新变量在基 $B$ 下的表示：$B^{-1}a_{n+1}$（即新列代入最优表的系数）。
  \item 计算检验数：
        \[
        \sigma_{n+1} = c_B^T B^{-1} a_{n+1} - c_{n+1}
        \]
  \item 若 $\sigma_{n+1} \le 0$（$\min$ 问题），原解仍最优，新变量不进基。
  \item 若 $\sigma_{n+1} > 0$，新变量进基，以当前表为起点继续单纯形迭代。
\end{enumerate}

\subsection{本章速查}

\begin{itemize}
  \item \textbf{对偶单纯形法}：保持 $\sigma \le 0$，消除 $b < 0$。先选最负 $b$ 离基 $\to$ 在负系数行上 $\min |\sigma|/|a|$ 选进基
  \item \textbf{与原始单纯形对称}：进基/离基顺序互换，比值方向互换（列 $\leftrightarrow$ 行）
  \item \textbf{非基变量 $c$ 变}：$\sigma_j' = \sigma_j - (c_j'-c_j)$，只改一个检验数。$\le 0$ 则不变，$>0$ 则进基继续迭代
  \item \textbf{基变量 $c$ 变}：$c_B$ 变了，所有非基 $\sigma_j' = (c_B')^T B^{-1}a_j - c_j$ 须重算，目标值 $z_0' = (c_B')^T B^{-1}b$ 也变
  \item \textbf{$b$ 变}：$\bar{b}' = B^{-1}b'$。若 $\ge 0$ $\to$ 基不变（仅取值变）；若有负 $\to$ 对偶单纯形法恢复
  \item \textbf{新增约束}：代入检查 $\to$ 不满足则添行消去基变量 $\to$ 对偶单纯形法
  \item \textbf{新增变量}：算 $B^{-1}a_{n+1}$ 和 $\sigma_{n+1} = c_B^T B^{-1}a_{n+1} - c_{n+1}$，$\le 0$ 则不变，$>0$ 则进基继续迭代
  \item \textbf{总决策原则}：先判断表的状态（原始可行/不可行 $\times$ 对偶可行/不可行），再选对应方法（原始单纯形/对偶单纯形/大M法重来）
\end{itemize}
